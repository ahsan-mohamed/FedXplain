# Points the frontend at your FastAPI backend.
# In dev, Vite's proxy (vite.config.ts) already forwards /api -> localhost:8000,
# so you usually don't need to set this locally. Set it explicitly for
# production builds pointing at a deployed backend URL.
VITE_API_BASE_URL=/api
